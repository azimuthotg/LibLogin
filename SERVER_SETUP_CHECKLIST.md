# ✅ LibLogin Server Setup Checklist

รายการสิ่งที่ต้องทำบน Windows Server ทีละขั้นตอน

---

## 📋 ก่อนเริ่มต้น - ข้อมูลที่ต้องเตรียม

จดข้อมูลเหล่านี้ไว้ก่อน:

- [ ] **Server IP Address**: `___.___.___.___` (เช่น 192.168.1.100)
- [ ] **MikroTik IP Address**: `___.___.___.___` (เช่น 192.168.1.1)
- [ ] **Admin Username**: `__________` (เช่น admin)
- [ ] **Admin Password**: `__________` (ตั้งรหัสที่จำง่าย แต่ปลอดภัย)

---

## 🔧 ส่วนที่ 1: ติดตั้ง Software (ทำครั้งเดียว)

### ขั้นตอนที่ 1.1: ติดตั้ง Python

1. [ ] ดาวน์โหลด Python 3.8+ จาก https://www.python.org/downloads/
2. [ ] รันโปรแกรมติดตั้ง
   - ✅ **สำคัญ**: ติ๊กถูก "Add Python to PATH"
   - คลิก "Install Now"
3. [ ] ทดสอบว่าติดตั้งสำเร็จ:
   ```powershell
   python --version
   pip --version
   ```
   ต้องเห็น Python 3.x และ pip

### ขั้นตอนที่ 1.2: ติดตั้ง Git

1. [ ] ดาวน์โหลด Git จาก https://git-scm.com/download/win
2. [ ] รันโปรแกรมติดตั้ง (กด Next ไปเรื่อยๆ ใช้ค่า default)
3. [ ] ทดสอบ:
   ```powershell
   git --version
   ```

### ขั้นตอนที่ 1.3: ติดตั้ง NSSM

1. [ ] ดาวน์โหลด NSSM จาก https://nssm.cc/download
2. [ ] แตกไฟล์ zip
3. [ ] คัดลอกไฟล์ `nssm.exe` จากโฟลเดอร์ `win64` ไปวางที่ `C:\Windows\System32\`
4. [ ] ทดสอบ:
   ```powershell
   nssm version
   ```

---

## 📁 ส่วนที่ 2: เตรียมโฟลเดอร์และ Clone Code

### ขั้นตอนที่ 2.1: สร้างโฟลเดอร์

1. [ ] เปิด File Explorer
2. [ ] ไปที่ `C:\`
3. [ ] สร้างโฟลเดอร์ `inetpub` (ถ้ายังไม่มี)
   - คลิกขวา → New → Folder → ตั้งชื่อ "inetpub"

### ขั้นตอนที่ 2.2: Clone Repository

1. [ ] เปิด PowerShell
2. [ ] รันคำสั่ง:
   ```powershell
   cd C:\inetpub
   git clone https://github.com/azimuthotg/LibLogin.git
   cd LibLogin
   ```
3. [ ] ตรวจสอบว่ามีไฟล์ในโฟลเดอร์:
   ```powershell
   dir
   ```
   ต้องเห็น: manage.py, backend, api, webapp, requirements.txt

---

## 🐍 ส่วนที่ 3: Setup Python Environment

### ขั้นตอนที่ 3.1: สร้าง Virtual Environment

```powershell
# ตรวจสอบว่าอยู่ใน C:\inetpub\LibLogin
pwd

# สร้าง venv
python -m venv venv

# เปิดใช้งาน venv
.\venv\Scripts\activate

# หน้าจอจะมี (venv) ข้างหน้า
```

- [ ] ทำตามคำสั่งด้านบน
- [ ] ตรวจสอบว่าเห็น `(venv)` หน้าคำสั่ง

### ขั้นตอนที่ 3.2: ติดตั้ง Dependencies

```powershell
# Upgrade pip
python -m pip install --upgrade pip

# ติดตั้ง packages ทั้งหมด
pip install -r requirements.txt
```

- [ ] รอให้ติดตั้งเสร็จ (อาจใช้เวลา 2-5 นาที)
- [ ] ตรวจสอบไม่มี error สีแดง

---

## ⚙️ ส่วนที่ 4: ตั้งค่า Django

### ขั้นตอนที่ 4.1: แก้ไข Settings

1. [ ] เปิดไฟล์ `backend\settings.py` ด้วย Notepad หรือ VS Code
2. [ ] หาบรรทัดที่มี `ALLOWED_HOSTS`
3. [ ] แก้เป็น:
   ```python
   ALLOWED_HOSTS = ['192.168.1.100', 'localhost', '127.0.0.1']
   ```
   **เปลี่ยน 192.168.1.100 เป็น IP จริงของ Server**

4. [ ] หาบรรทัดที่มี `CORS_ALLOWED_ORIGINS`
5. [ ] แก้เป็น:
   ```python
   CORS_ALLOWED_ORIGINS = [
       "http://192.168.1.1",      # MikroTik IP
       "http://192.168.1.100",    # Server IP
   ]
   ```
   **เปลี่ยน IP เป็นค่าจริง**

6. [ ] หาบรรทัดที่มี `CSRF_TRUSTED_ORIGINS`
7. [ ] แก้เป็น:
   ```python
   CSRF_TRUSTED_ORIGINS = [
       'http://192.168.1.100',
       'http://192.168.1.100:8000',
   ]
   ```
   **เปลี่ยน IP เป็นค่าจริง**

8. [ ] **สำคัญมาก**: เปลี่ยน `SECRET_KEY`
   - รันคำสั่งนี้ใน PowerShell:
     ```powershell
     python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
     ```
   - คัดลอก key ที่ได้
   - แทนที่ใน `SECRET_KEY = 'คัดลอก-key-ใหม่-มาใส่ตรงนี้'`

9. [ ] **Production**: เปลี่ยน `DEBUG = False`
   ```python
   DEBUG = False  # สำหรับ production
   # DEBUG = True   # สำหรับ development
   ```

10. [ ] บันทึกไฟล์ (Ctrl+S)

### ขั้นตอนที่ 4.2: Setup Database

```powershell
# ตรวจสอบว่า venv ยังเปิดอยู่ (ต้องมี (venv) หน้าคำสั่ง)
# ถ้าปิดไป ใช้: .\venv\Scripts\activate

# Run migrations
python manage.py migrate

# สร้าง superuser
python manage.py createsuperuser
```

- [ ] รัน `migrate` สำเร็จ
- [ ] กรอกข้อมูล superuser:
  - Username: `admin` (หรือตามต้องการ)
  - Email: `admin@library.local` (หรือตามต้องการ)
  - Password: `********` (ตั้งรหัสที่จำได้)
  - Confirm password: `********`
- [ ] จดข้อมูล username และ password ไว้!

### ขั้นตอนที่ 4.3: Collect Static Files

```powershell
python manage.py collectstatic --noinput
```

- [ ] รอให้ copy ไฟล์เสร็จ

---

## 🧪 ส่วนที่ 5: ทดสอบรันระบบ

### ขั้นตอนที่ 5.1: ทดสอบรันด้วย Waitress

```powershell
# รันเซิร์ฟเวอร์
python run_server.py
```

ควรเห็น:
```
============================================================
LibLogin - Library WiFi Login Management System
============================================================
Starting Waitress WSGI Server...
Host: 0.0.0.0
Port: 8000
URL: http://localhost:8000
------------------------------------------------------------
Press Ctrl+C to quit
============================================================
```

- [ ] เห็นข้อความด้านบน (ไม่มี error)

### ขั้นตอนที่ 5.2: ทดสอบเข้าเว็บ

**บน Server (เครื่องเดียวกัน):**

1. [ ] เปิด Browser (Chrome, Edge)
2. [ ] ไปที่ `http://localhost:8000/login/`
3. [ ] ต้องเห็นหน้า Login
4. [ ] ลอง Login ด้วย username/password ที่สร้างไว้
5. [ ] ถ้า Login สำเร็จ จะเข้า Dashboard

**จากเครื่องอื่นในเครือข่ายเดียวกัน:**

1. [ ] เปิด Browser
2. [ ] ไปที่ `http://192.168.1.100:8000/login/` (เปลี่ยน IP ตามจริง)
3. [ ] ถ้าเข้าได้ = สำเร็จ!
4. [ ] ถ้าเข้าไม่ได้ = ต้องตั้งค่า Firewall (ดูขั้นตอนถัดไป)

### ขั้นตอนที่ 5.3: หยุดเซิร์ฟเวอร์

- [ ] กลับไปที่ PowerShell
- [ ] กด `Ctrl+C` เพื่อหยุดเซิร์ฟเวอร์

---

## 🔥 ส่วนที่ 6: ตั้งค่า Firewall

### ขั้นตอนที่ 6.1: เปิด Port 8000

**วิธีที่ 1: ใช้ PowerShell (แนะนำ)**

1. [ ] เปิด PowerShell แบบ **Run as Administrator**
2. [ ] รันคำสั่ง:
   ```powershell
   New-NetFirewallRule -DisplayName "LibLogin HTTP" -Direction Inbound -LocalPort 8000 -Protocol TCP -Action Allow
   ```
3. [ ] ต้องเห็น "OK" หรือไม่มี error

**วิธีที่ 2: ใช้ GUI**

1. [ ] เปิด **Windows Defender Firewall with Advanced Security**
2. [ ] คลิก **Inbound Rules** ทางซ้าย
3. [ ] คลิก **New Rule** ทางขวา
4. [ ] เลือก **Port** → Next
5. [ ] เลือก **TCP** → Specific local ports: `8000` → Next
6. [ ] เลือก **Allow the connection** → Next
7. [ ] เลือกทั้งหมด (Domain, Private, Public) → Next
8. [ ] ตั้งชื่อ: `LibLogin HTTP` → Finish

### ขั้นตอนที่ 6.2: ทดสอบอีกครั้ง

1. [ ] รันเซิร์ฟเวอร์อีกครั้ง: `python run_server.py`
2. [ ] ลองเข้าจากเครื่องอื่น: `http://192.168.1.100:8000/login/`
3. [ ] ต้องเข้าได้

---

## 🎯 ส่วนที่ 7: ติดตั้งเป็น Windows Service (รันอัตโนมัติ)

### ขั้นตอนที่ 7.1: สร้างโฟลเดอร์ Logs

```powershell
# ใน PowerShell
cd C:\inetpub\LibLogin
mkdir logs
```

- [ ] สร้างโฟลเดอร์ logs สำเร็จ

### ขั้นตอนที่ 7.2: ติดตั้ง Service ด้วย NSSM

1. [ ] เปิด PowerShell แบบ **Run as Administrator**
2. [ ] รันคำสั่ง:
   ```powershell
   nssm install LibLogin
   ```
3. [ ] หน้าต่าง NSSM จะเปิดขึ้น

**Application Tab:**
- [ ] **Path**: คลิก `...` เลือกไฟล์ `C:\inetpub\LibLogin\venv\Scripts\python.exe`
- [ ] **Startup directory**: พิมพ์ `C:\inetpub\LibLogin`
- [ ] **Arguments**: พิมพ์ `run_server.py`

**Details Tab:**
- [ ] **Display name**: `LibLogin Service`
- [ ] **Description**: `Library WiFi Login Management System`
- [ ] **Startup type**: เลือก `Automatic`

**I/O Tab:**
- [ ] **Output (stdout)**: พิมพ์ `C:\inetpub\LibLogin\logs\output.log`
- [ ] **Error (stderr)**: พิมพ์ `C:\inetpub\LibLogin\logs\error.log`

4. [ ] คลิก **Install service**
5. [ ] ต้องเห็นข้อความว่า "Service installed successfully!"

### ขั้นตอนที่ 7.3: Start Service

```powershell
# เริ่มต้น Service
nssm start LibLogin

# ตรวจสอบสถานะ
nssm status LibLogin
```

- [ ] สถานะต้องเป็น "SERVICE_RUNNING"

### ขั้นตอนที่ 7.4: ทดสอบ Service

1. [ ] เปิด Browser ไปที่ `http://localhost:8000/login/`
2. [ ] ต้องเข้าได้ (แม้ไม่ได้รัน `python run_server.py`)
3. [ ] ลอง Restart Server (เครื่องคอมพิวเตอร์)
4. [ ] หลัง Restart เปิด Browser ทดสอบอีกครั้ง
5. [ ] ถ้าเข้าได้ = Service รันอัตโนมัติสำเร็จ!

---

## 📱 ส่วนที่ 8: อัปโหลดรูปพื้นหลังทดสอบ

### ขั้นตอนที่ 8.1: เข้าระบบ

1. [ ] เปิด Browser ไปที่ `http://192.168.1.100:8000/login/`
2. [ ] Login ด้วย admin username/password
3. [ ] เห็น Dashboard

### ขั้นตอนที่ 8.2: อัปโหลดรูป

1. [ ] คลิกเมนู **Background Images** ด้านซ้าย
2. [ ] คลิกปุ่ม **+ Upload New Background**
3. [ ] กรอกข้อมูล:
   - **Title**: `ทดสอบรูปพื้นหลัง`
   - **Router ID**: เว้นว่าง (สำหรับทดสอบ)
   - **Choose File**: เลือกรูป JPG/PNG ที่ต้องการ
   - **Set as Active**: ✅ ติ๊กถูก
4. [ ] คลิก **Upload**
5. [ ] รอสักครู่ รูปจะแสดงในรายการ

### ขั้นตอนที่ 8.3: ทดสอบ API

1. [ ] เปิด Tab ใหม่
2. [ ] ไปที่ `http://192.168.1.100:8000/api/login-background/`
3. [ ] ต้องเห็น JSON:
   ```json
   {
     "success": true,
     "imageUrl": "http://192.168.1.100:8000/media/backgrounds/xxx.jpg",
     "title": "ทดสอบรูปพื้นหลัง"
   }
   ```
4. [ ] คลิกที่ imageUrl ต้องเห็นรูปที่อัปโหลด

---

## 🌐 ส่วนที่ 9: ตั้งค่า MikroTik (ขั้นตอนสุดท้าย)

### ขั้นตอนที่ 9.1: แก้ไขไฟล์ Login Page

1. [ ] ไปที่โฟลเดอร์ `C:\inetpub\LibLogin`
2. [ ] เปิดไฟล์ `mikrotik_login.html` ด้วย Notepad
3. [ ] หาบรรทัด:
   ```javascript
   const API_BASE_URL = 'http://YOUR_SERVER_IP:8000';
   ```
4. [ ] เปลี่ยนเป็น:
   ```javascript
   const API_BASE_URL = 'http://192.168.1.100:8000';  // ใส่ IP จริงของ Server
   ```
5. [ ] ถ้าต้องการตั้ง Router ID:
   ```javascript
   const ROUTER_ID = 'FLOOR1';  // เช่น ชั้น 1 ใช้ FLOOR1, ชั้น 2 ใช้ FLOOR2
   ```
   หรือเว้นว่าง:
   ```javascript
   const ROUTER_ID = '';  // ใช้รูปพื้นหลังทั่วไป
   ```
6. [ ] บันทึกไฟล์

### ขั้นตอนที่ 9.2: อัปโหลดไปยัง MikroTik

**ผ่าน WinBox:**

1. [ ] เปิด WinBox
2. [ ] เชื่อมต่อกับ MikroTik Router
3. [ ] คลิก **Files** ทางซ้าย
4. [ ] ลากไฟล์ `mikrotik_login.html` จาก `C:\inetpub\LibLogin` วางลงใน Files
5. [ ] รอให้อัปโหลดเสร็จ
6. [ ] คลิกขวาที่ไฟล์ → Rename → เปลี่ยนชื่อเป็น `login.html`
7. [ ] ลบไฟล์ `login.html` เก่า (ถ้ามี) และเปลี่ยนชื่อไฟล์ใหม่

### ขั้นตอนที่ 9.3: ทดสอบหน้า Login MikroTik

1. [ ] เชื่อมต่อ WiFi ของ MikroTik (จากมือถือหรือคอมพิวเตอร์)
2. [ ] เปิด Browser (จะถูกนำไปหน้า Login อัตโนมัติ)
3. [ ] ตรวจสอบ:
   - [ ] เห็นหน้า Login ภาษาไทย "ยินดีต้อนรับ"
   - [ ] เห็นรูปพื้นหลังที่อัปโหลดไว้
   - [ ] มีช่องกรอก Username และ Password
4. [ ] ทดสอบ Login (ถ้ามี user ใน MikroTik)

---

## ✅ เสร็จสิ้น! ตรวจสอบสิ่งเหล่านี้

### Final Checklist:

- [ ] Service รันอัตโนมัติเมื่อ Server เปิด
- [ ] เข้า Web Interface ได้จากเครื่องอื่น
- [ ] API ตอบกลับถูกต้อง
- [ ] อัปโหลดรูปพื้นหลังได้
- [ ] MikroTik Login Page แสดงรูปพื้นหลังถูกต้อง
- [ ] Login WiFi ได้ปกติ

---

## 📊 ข้อมูลสำคัญที่ต้องจำ

| รายการ | ค่า |
|--------|-----|
| **Server IP** | _____________ |
| **Web Interface URL** | http://[server-ip]:8000/login/ |
| **API Endpoint** | http://[server-ip]:8000/api/login-background/ |
| **Admin Username** | _____________ |
| **Admin Password** | _____________ |
| **MikroTik IP** | _____________ |

---

## 🆘 หากมีปัญหา

### Service ไม่ start
```powershell
type C:\inetpub\LibLogin\logs\error.log
```

### เข้าไม่ได้จากเครื่องอื่น
```powershell
# ตรวจสอบ Firewall
Get-NetFirewallRule -DisplayName "LibLogin HTTP"

# ตรวจสอบ Service
nssm status LibLogin
```

### API ไม่ตอบกลับ
```powershell
# Restart Service
nssm restart LibLogin

# ดู logs
type C:\inetpub\LibLogin\logs\output.log
```

---

## 📞 การจัดการ Service

### คำสั่งที่ใช้บ่อย

```powershell
# เริ่มต้น
nssm start LibLogin

# หยุด
nssm stop LibLogin

# รีสตาร์ท
nssm restart LibLogin

# ดูสถานะ
nssm status LibLogin

# แก้ไขการตั้งค่า
nssm edit LibLogin
```

---

**🎉 สำเร็จ! ระบบพร้อมใช้งาน**

หากต้องการเปลี่ยนรูปพื้นหลัง:
1. เข้า http://[server-ip]:8000/login/
2. Login
3. คลิก Background Images
4. อัปโหลดรูปใหม่และกด Activate

**ไม่ต้องรีสตาร์ท MikroTik หรือ Server!**

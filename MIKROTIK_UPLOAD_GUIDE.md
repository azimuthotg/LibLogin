# 📝 คู่มือการอัพโหลดไฟล์ไปบน MikroTik

## 🎯 ภาพรวม

ไฟล์ที่ต้องอัพโหลด: `/hotspot/login.html` (ไฟล์เดียว)
- อัพโหลดไปทับไฟล์ `login.html` เดิมบน MikroTik ทั้ง 2 ตัว

---

## 🖥️ MikroTik Routers

### **MikroTik #1**
- **IP Address**: `202.29.55.180`
- **Router ID**: `mt1`
- **ไฟล์**: `/hotspot/login.html` (เหมือนกัน)

### **MikroTik #2**
- **IP Address**: `202.29.55.30`
- **Router ID**: `mt2`
- **ไฟล์**: `/hotspot/login.html` (เหมือนกัน)

---

## 📋 ขั้นตอนการอัพโหลดไฟล์

### **วิธีที่ 1: ใช้ WinBox (แนะนำ)**

#### 1. เปิด WinBox และเชื่อมต่อ MikroTik
```
- เปิด WinBox
- Connect ไปยัง 202.29.55.180 (MT1) หรือ 202.29.55.30 (MT2)
- ใส่ username/password
```

#### 2. เปิด File Manager
```
Menu → Files
```

#### 3. ค้นหาไฟล์ login.html เดิม
```
- หาโฟลเดอร์ hotspot/
- หาไฟล์ login.html
```

#### 4. ลบไฟล์เดิม (หรือ rename เป็น login.html.backup)
```
- คลิกขวาที่ login.html
- เลือก "Remove" หรือ "Rename" → login.html.backup
```

#### 5. อัพโหลดไฟล์ใหม่
```
- คลิก "Upload" button
- เลือกไฟล์: /mnt/c/claude-test/LibLogin/hotspot/login.html
- รอจนอัพโหลดเสร็จ (100%)
```

#### 6. ย้ายไฟล์เข้าโฟลเดอร์ hotspot
```
- ไฟล์ที่อัพโหลดจะอยู่ที่ root
- Drag & Drop ไฟล์ login.html เข้าไปใน hotspot/
```

#### 7. ทดสอบ
```
- เชื่อมต่อ WiFi hotspot
- เปิดเบราว์เซอร์ → หน้า login จะแสดงขึ้นพร้อมพื้นหลังจาก API
```

---

### **วิธีที่ 2: ใช้ FTP**

#### 1. เปิดใช้งาน FTP บน MikroTik
```
/ip service
set ftp disabled=no
```

#### 2. เชื่อมต่อผ่าน FTP Client (เช่น FileZilla)
```
Host: 202.29.55.180 (หรือ 202.29.55.30)
Username: admin (หรือ username ของคุณ)
Password: (รหัสผ่าน)
Port: 21
```

#### 3. อัพโหลดไฟล์
```
- เข้าโฟลเดอร์ hotspot/
- ลบหรือ rename login.html เดิม
- อัพโหลด login.html ใหม่
```

#### 4. ปิด FTP (เพื่อความปลอดภัย)
```
/ip service
set ftp disabled=yes
```

---

### **วิธีที่ 3: ใช้ Terminal/SSH**

#### 1. SSH เข้า MikroTik
```bash
ssh admin@202.29.55.180
# หรือ
ssh admin@202.29.55.30
```

#### 2. Backup ไฟล์เดิม
```
/file
print
# หา login.html ในโฟลเดอร์ hotspot

# Rename เดิม (ถ้าต้องการ backup)
set [find name="hotspot/login.html"] name="hotspot/login.html.backup"
```

#### 3. อัพโหลดไฟล์ใหม่ผ่าน FTP หรือ SCP
```bash
# ใช้ SCP จาก local machine
scp /mnt/c/claude-test/LibLogin/hotspot/login.html admin@202.29.55.180:hotspot/
```

---

## ✅ ตรวจสอบหลังอัพโหลด

### 1. ตรวจสอบไฟล์บน MikroTik
```
WinBox → Files → hotspot/login.html
- ขนาดไฟล์ควรประมาณ 5-6 KB
- วันที่แก้ไขล่าสุดควรเป็นวันนี้
```

### 2. ทดสอบการทำงาน
```
1. เชื่อมต่อ WiFi hotspot
2. เปิดเบราว์เซอร์ใดก็ได้
3. จะถูก redirect ไปหน้า login
4. เปิด Developer Console (F12)
5. ดู Console log:
   - ควรเห็น "Fetching background from: http://202.29.55.222:8291/api/login-background/"
   - ควรเห็น "Background loaded: [ชื่อรูป]"
```

### 3. ตรวจสอบพื้นหลัง
```
- หน้า login ควรแสดงรูปพื้นหลังจาก Django API
- ถ้าไม่มีรูป จะแสดงพื้นหลังว่างเปล่า (ไม่ error)
- Form login ยังทำงานปกติ
```

---

## 🔧 Troubleshooting

### ปัญหา: ไม่มีพื้นหลังแสดง

**สาเหตุที่เป็นไปได้:**

1. **ไม่มี Walled Garden**
   ```
   /ip hotspot walled-garden
   add dst-host=202.29.55.222 comment="Django API Server"
   ```

2. **ไม่มีรูป active ในฐานข้อมูล**
   ```
   - เข้า http://202.29.55.222:8291/backgrounds/
   - อัพโหลดรูป และ set active
   ```

3. **Django server ไม่ได้รัน**
   ```bash
   cd /mnt/c/claude-test/LibLogin
   python manage.py runserver 0.0.0.0:8291
   ```

4. **CORS ไม่ถูกต้อง**
   ```python
   # backend/settings.py ต้องมี:
   CORS_ALLOW_ALL_ORIGINS = True
   ```

### ปัญหา: Form login ไม่ทำงาน

**ตรวจสอบ:**
```
- ไฟล์ login.html ไม่เสียหาย
- ไฟล์ CSS และ JS อื่นๆ ยังอยู่ครบ (css/style.css, md5.js)
- MikroTik variables ยังทำงาน ($(link-login-only), $(username))
```

### ปัญหา: API Error

**เปิด Developer Console (F12) ดู Error:**
```javascript
// CORS Error
// แก้: ตรวจสอบ CORS settings ใน Django

// Network Error
// แก้: ตรวจสอบว่า Django server รันอยู่

// 404 Not Found
// แก้: ตรวจสอบว่ามีรูป active ในฐานข้อมูล
```

---

## 📊 ตรวจสอบการทำงานผ่าน Test Page

### เปิดหน้าทดสอบ:
```
http://202.29.55.222:8291/test_hotspot_background.html
```

### ทดสอบ:
1. ✅ กด "ทดสอบ API (Default)" → ควรได้ JSON response
2. ✅ กด "ทดสอบ API (router_id=mt1)" → ควรได้รูปสำหรับ MT1
3. ✅ กด "ทดสอบ API (router_id=mt2)" → ควรได้รูปสำหรับ MT2
4. ✅ กด "โหลดพื้นหลัง" → ควรเห็นรูปแสดงด้านล่าง

---

## 🔐 Walled Garden Configuration

**ต้องตั้งค่าบนทั้ง 2 MikroTik:**

```bash
# MikroTik #1 (202.29.55.180)
/ip hotspot walled-garden
add dst-host=202.29.55.222 comment="Django API Server"

# MikroTik #2 (202.29.55.30)
/ip hotspot walled-garden
add dst-host=202.29.55.222 comment="Django API Server"
```

**เหตุผล**:
- ให้ JavaScript สามารถ fetch API จาก `202.29.55.222` ได้โดยไม่ต้อง login ก่อน
- ถ้าไม่มี Walled Garden จะ fetch ไม่ได้ (blocked)

---

## 📁 โครงสร้างไฟล์บน MikroTik

```
MikroTik Files:
/
├── hotspot/
│   ├── login.html          ← ไฟล์ที่อัพโหลดใหม่ (แก้ไขแล้ว)
│   ├── logout.html         (เดิม ไม่ต้องแก้)
│   ├── status.html         (เดิม ไม่ต้องแก้)
│   ├── error.html          (เดิม ไม่ต้องแก้)
│   ├── css/
│   │   └── style.css       (เดิม ไม่ต้องแก้)
│   ├── img/
│   │   ├── user.svg        (เดิม ไม่ต้องแก้)
│   │   └── password.svg    (เดิม ไม่ต้องแก้)
│   └── md5.js              (เดิม ไม่ต้องแก้)
└── ...
```

**หมายเหตุ**: อัพโหลดเฉพาะ `login.html` เท่านั้น ไฟล์อื่นไม่ต้องแก้ไข

---

## 🎯 สรุปขั้นตอน

1. ✅ Backup ไฟล์เดิม (login.html.backup)
2. ✅ อัพโหลด login.html ใหม่ผ่าน WinBox/FTP
3. ✅ ตั้งค่า Walled Garden (ถ้ายังไม่มี)
4. ✅ ทดสอบหน้า login (เชื่อมต่อ WiFi)
5. ✅ ตรวจสอบ Console log (F12)
6. ✅ ยืนยันว่ามีพื้นหลังแสดง

---

## 🆘 ติดต่อสอบถาม

หากพบปัญหา:
1. เช็ค Console log (F12) → มี error อะไร
2. ทดสอบ API ที่ http://202.29.55.222:8291/test_hotspot_background.html
3. ตรวจสอบว่า Django server รันอยู่
4. ตรวจสอบ Walled Garden configuration

---

**วันที่สร้าง**: 2025-11-12
**เวอร์ชัน**: Phase 1 - Dynamic Background Only

# MikroTik Redirect Files

ไฟล์ขนาดเล็กสำหรับอัปโหลดไปยัง MikroTik เพื่อ redirect ไปยัง Django Server

---

## 📁 ไฟล์ทั้งหมด

| ไฟล์ | จุดประสงค์ | ขนาด |
|------|-----------|------|
| `login.html` | Redirect ไปหน้า Login | ~1 KB |
| `logout.html` | Redirect ไปหน้า Logout | ~1 KB |
| `status.html` | Redirect ไปหน้า Status | ~1 KB |
| `error.html` | Redirect ไปหน้า Error | ~1 KB |

---

## 🔧 วิธีใช้งาน

### 1. แก้ไข IP Address

เปิดแก้ไขไฟล์ทุกไฟล์ แล้วเปลี่ยน:
```
http://202.29.55.222:8291
```
เป็น IP Address จริงของ Server

### 2. อัปโหลดไปยัง MikroTik

**ผ่าน WinBox:**
1. เปิด WinBox → เชื่อมต่อกับ Router
2. ไปที่ **Files**
3. ลากไฟล์ทั้ง 4 ไฟล์วางลงใน MikroTik
4. รอให้อัปโหลดเสร็จ

**ผ่าน FTP:**
```bash
ftp 192.168.1.1
Username: admin
Password: ********

put login.html
put logout.html
put status.html
put error.html
quit
```

### 3. ตั้งค่า Hotspot Profile

```
/ip hotspot profile
set [find default=yes] html-directory=hotspot
```

### 4. เพิ่ม Walled Garden

เพิ่ม Server IP ลงใน Walled Garden เพื่อให้เข้าถึงได้ก่อน Login:

```
/ip hotspot walled-garden
add dst-host=202.29.55.222 comment="Django Login Server"
```

---

## ✅ ทดสอบ

1. เชื่อมต่อ WiFi ของ MikroTik
2. เปิด Browser
3. ควรถูก redirect ไปยัง Django Server
4. เห็นหน้า Login ที่สวยงาม พร้อมรูปพื้นหลัง

---

## 🎯 ข้อดี

- ✅ ไฟล์เล็กมาก (~1 KB ต่อไฟล์)
- ✅ ไม่กิน Storage ของ MikroTik
- ✅ แก้ไขหน้าตาได้ที่ Django Server
- ✅ อัปเดตง่าย ไม่ต้องแตะ MikroTik

---

## 📝 หมายเหตุ

- ไฟล์เหล่านี้เป็นแค่ redirect เท่านั้น
- เนื้อหาจริงอยู่ที่ Django Server
- แก้ไขอะไรก็ได้ที่ Server ไม่ต้องแตะ MikroTik อีก
